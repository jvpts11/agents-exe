import System.IO.Console;
program AgentsExe;

// The biological one, and the whole of what it decides: who can gestate.
//
// SEALED, so the day anything matches on it, every case is named. It is two constants and it is
// going to stay two -- which is exactly when sealing costs nothing and is worth writing.
public bundle Agents {
    public namespace People {

        public sealed enum Sex permits
            Female,
            Male;
    }
}
